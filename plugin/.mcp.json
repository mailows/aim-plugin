{
  "mcpServers": {
    "aim": {
      "command": "uvx",
      "args": [
        "--from",
        "aimessenger==0.1.8",
        "aim",
        "channel"
      ],
      "env": {
        "AIM_RELAY_URL": "${user_config.relay_url}"
      }
    }
  }
}
